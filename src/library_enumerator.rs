// Contains code for a subdomain enumerator that finds subdomains by trying 
// subdomains from a library of common subdomains 

// atkes a domain as a string and a path to a library as a string and returns 
pub fn enumerate(domain: &String, library: &String) -> Vec<String> {
    Vec::new() 
}